/*
    Removes the first element from an array and returns it. If
    the array is empty, undefined is returned and the array is not modified.
*/

const pairNumbers = [9, 10, 22, 18, 40]

pairNumbers.shift()

console.log(pairNumbers) // > [10, 22, 18, 40]
